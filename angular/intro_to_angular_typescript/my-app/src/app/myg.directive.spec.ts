import { MygDirective } from './myg.directive';

describe('MygDirective', () => {
  it('should create an instance', () => {
    const directive = new MygDirective();
    expect(directive).toBeTruthy();
  });
});
