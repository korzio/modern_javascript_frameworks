import { WakaWakaDirective } from './waka-waka.directive';

describe('WakaWakaDirective', () => {
  it('should create an instance', () => {
    const directive = new WakaWakaDirective();
    expect(directive).toBeTruthy();
  });
});
